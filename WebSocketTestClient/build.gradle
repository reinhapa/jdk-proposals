apply plugin: 'application'

dependencies {
    compile "javax.websocket:javax.websocket-client-api:1.+"
    compile "org.eclipse.jetty.websocket:javax-websocket-client-impl:9.+"
}

mainClassName = "ws.TestClient"

jar.manifest {
    attributes 'Main-Class': 'ws.TestClient'
}
